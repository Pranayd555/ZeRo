import { CustomLoaderDirective } from './custom-loader.directive';

describe('CustomLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
